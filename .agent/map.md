# Map - codebase wiring (grows with the build)

Fresh rebuild; modules land per phase. Keep current: helpers load order, the
data -> module -> output flow, and any cache producer -> consumer pairs.

(empty - P0 adds the first entries: env, the helpers loader, data IO + design.)
